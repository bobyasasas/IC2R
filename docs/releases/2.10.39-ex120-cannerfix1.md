恢复并发布 IC2R `2.10.39-ex120` 源码，包含 `cannerfix1` 罐装机修复和后续可读性整理。

适用环境：Minecraft **1.20.1**、Forge **47.4.20**、Java **17**。
模组内部版本保留为 `2.10.39-ex120`；发布标签和文件名中的 `cannerfix1` 标识包含的修复。

本次恢复包含炸药与遥控器、收纳盒、补水单元、Jade 集成，以及电网、反应堆、流体容器、UU 复制和配方等版本差异。
罐装机在切换模式时会刷新对应音效。96 个恢复文件已整理命名、常量引用、条件分支和格式。

下载 `ic2-forge-2.10.39-ex120-cannerfix1.jar` 作为模组文件；`sources.zip` 是完整项目源码，不是游戏模组。
`SHA256SUMS.txt` 提供两个附件的 SHA-256 校验值。

发布附件由本标签上的 GitHub Actions 执行 `clean build` 后生成。
本地已进行构建和原包静态比对：1,299 个类、5,829 个资源全部对应，声明及引用计数检查通过。
这不是游戏内运行测试，反编译无法完整还原注释及所有原始写法。详细恢复记录见源码中的 `RECOVERY.md` 与 `READABILITY.md`。
